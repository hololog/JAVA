package for_statement;

public class Ex2 {

	public static void main(String[] args) {
		/*
		 * 반복문을 사용한 연산의 누적
		 * - 특정 연산 결과를 누적하는 경우
		 *   ex) 점수의 합 계산 등
		 * - 연산 결과를 누적하는 변수는 반드시 반복문보다 앞(위)에 선언 및 초기화 수행
		 * */
		
		
		// 1 ~ 100까지 합
		// 정수(i) 1 ~ 100 까지 1씩 증가하면서 정수(i)의 합(sum)을 계산
		// 정수의 합을 누적할 누적변수 sum은 반드시 for문보다 윗쪽에 선언, 초기화 필수!
		// => 만약, 초기화를 수행하지 않은 채로 합계를 누적하는 경우 오류가 발행하므로
		//    반드시 누적변수는 변수의 선언과 함께 초기화 필수!
		int sum = 0;
		for(int i=1; i<=10000; i++) {
			sum += i;
		}
		System.out.println("정수 1~100 까지의 합: " + sum);
		System.out.println("===========================");
		
		/*
		 * 반복문과 조건문의 결합
		 * - 반복문 내에서 조건문을 통해 특정 조건일 때만 일부 반복문을 수행 제어 가능
		 *   ex) 1 ~ 100 까지 1씩 증가하면서 홀수와 짝수의 합을 따로 누적
		 * */
		
		// 1 ~ 10까지 1씩 증가하면서 짝수의 합만 누적 후 출력(30)
		int sum2 = 0;
		for(int i=1; i<=10; i++) {
			
		}
		
		
		
		
		
		
		
		
	}

}
